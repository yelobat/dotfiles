# dotfiles

These are my dotfiles for my EndeavourOS configuration. It uses Niri as the
scrolling WM, Doom Emacs as the text editor, Alacritty as the terminal emulator
and noctalia-shell as the wayland desktop shell.
